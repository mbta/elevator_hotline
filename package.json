{
  "scripts": {
    "local": "node test/local.js",
    "prettier": "prettier --write src test index.js",
    "prettier:check": "prettier --check src test index.js",
    "test": "jest --setupFiles ./.env"
  },
  "dependencies": {
    "@sentry/node": "^5.17.0",
    "aws-sdk": "^2.814.0"
  },
  "license": "MIT",
  "devDependencies": {
    "aws-lambda-pify": "^3.0.0",
    "dotenv": "^8.2.0",
    "jest": "^26.0.1",
    "prettier": "2.0.5"
  }
}
